#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a , b ;
    printf("enter two number\t");
    scanf("%f%f",&a,&b);
    if (a > b)
    {
         printf("the mini no is = \t %f",b);

    }
    else
    {
        printf("the mini no is = \t %f",a);
    }

}
